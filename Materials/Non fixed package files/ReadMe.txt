The package.json files used in this course have been set to depend on specific versions of npm packages in order to avoid breaking changes that might cause issues during a course.

In the real world, development versions of package.json files are NOT fixed - so included here are the original non-fixed versions of the package.json files.